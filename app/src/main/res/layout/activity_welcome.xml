<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_welcome_gradient">

    <!-- Líneas decorativas diagonales (capa visual) -->
    <View
        android:id="@+id/decorLines"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_diagonal_lines"
        android:alpha="0.08"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Título principal -->
    <TextView
        android:id="@+id/tvWelcomeTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Bienvenido a Deporte Connect, vamos a configurar tu perfil."
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/cardLegalWelcome"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:layout_constraintVertical_bias="0.42" />

    <!-- Tarjeta Aviso Legal -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardLegalWelcome"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:layout_marginEnd="28dp"
        android:layout_marginTop="32dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#1E3A8A"
        app:layout_constraintTop_toBottomOf="@id/tvWelcomeTitle"
        app:layout_constraintBottom_toTopOf="@id/btnNext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:gravity="center_horizontal">

            <!-- Ícono chevron doble (decorativo) -->
            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@drawable/ic_chevron_double"
                app:tint="@color/dc_green"
                android:contentDescription=""
                android:layout_marginBottom="10dp" />

            <!-- Título aviso -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Aviso Legal Importante"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@color/dc_green"
                android:layout_marginBottom="10dp" />

            <!-- Cuerpo aviso -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Recuerda que esta app es para mayores de 18 años. Al continuar, confirmas que cumples con este requisito de edad."
                android:textSize="14sp"
                android:textColor="#CCffffff"
                android:gravity="center"
                android:lineSpacingMultiplier="1.4" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Botón Siguiente -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnNext"
        android:layout_width="wrap_content"
        android:layout_height="52dp"
        android:text="Siguiente  →"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:paddingStart="36dp"
        android:paddingEnd="36dp"
        android:layout_marginTop="36dp"
        android:layout_marginBottom="60dp"
        app:backgroundTint="#1E3A8A"
        app:cornerRadius="30dp"
        app:strokeColor="#3B82F6"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toBottomOf="@id/cardLegalWelcome"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.3" />

</androidx.constraintlayout.widget.ConstraintLayout>
